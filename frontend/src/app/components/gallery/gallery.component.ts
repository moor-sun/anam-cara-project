import { CommonModule } from '@angular/common';import { Component,HostListener,OnInit } from '@angular/core';import { ContentService,SiteContent } from '../../services/content.service';
@Component({standalone:true,imports:[CommonModule],styles:[`
.gallery-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(170px,1fr));gap:16px;align-items:start}.gallery-item{position:relative;margin:0;height:170px;background:#fff;border:1px solid #eadfc9;border-radius:16px;overflow:hidden;box-shadow:0 6px 16px #0001;cursor:pointer}.gallery-item img,.gallery-item video{display:block;width:100%;height:100%;object-fit:cover;background:#211}.gallery-item figcaption{position:absolute;inset:auto 0 0;padding:24px 10px 9px;color:#fff;font-weight:700;background:linear-gradient(transparent,#000b);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.lightbox{position:fixed;inset:0;z-index:100;background:#000d;display:flex;align-items:center;justify-content:center;padding:40px;cursor:zoom-out}.lightbox img,.lightbox video{max-width:95vw;max-height:88vh;object-fit:contain}.lightbox-caption{position:absolute;bottom:8px;color:#fff}.close{position:absolute;right:18px;top:16px;font:32px Arial;background:transparent!important;padding:4px 10px}@media(max-width:600px){.gallery-grid{grid-template-columns:repeat(2,1fr);gap:10px}.gallery-item{height:145px}.lightbox{padding:20px}}
`],template:`<section class="page-hero"><span class="eyebrow">Moments from Anam Cara</span><h1>Gallery</h1><p class="lead">Select any photo or video to view its original size.</p></section><section class="section gallery-grid" *ngIf="items.length;else empty"><figure class="gallery-item" *ngFor="let item of items" (click)="open(item)"><img *ngIf="item.mediaType==='IMAGE'" [src]="media(item)" [alt]="item.title||'Gallery photo'"><video *ngIf="item.mediaType==='VIDEO'" [src]="media(item)" preload="metadata"></video><figcaption *ngIf="item.title">{{item.title}}</figcaption></figure></section><div class="lightbox" *ngIf="selected" (click)="close()"><button class="close" type="button" aria-label="Close">×</button><img *ngIf="selected.mediaType==='IMAGE'" [src]="media(selected)" [alt]="selected.title"><video *ngIf="selected.mediaType==='VIDEO'" [src]="media(selected)" controls autoplay (click)="$event.stopPropagation()"></video><div class="lightbox-caption" *ngIf="selected.title">{{selected.title}}</div></div><ng-template #empty><section class="section"><div class="card">Gallery photos and videos will appear here soon.</div></section></ng-template>`})
export class GalleryComponent implements OnInit{items:SiteContent[]=[];selected?:SiteContent;constructor(private content:ContentService){}ngOnInit(){this.content.all().subscribe({next:v=>this.items=v.filter(i=>i.contentType==='GALLERY')});}media(i:SiteContent){return this.content.mediaUrl(i.mediaUrl);}open(i:SiteContent){this.selected=i;}close(){this.selected=undefined;}@HostListener('document:keydown.escape')escape(){this.close();}}
